import { fileIconBody } from './file-extension-icon';
import { IconDef } from './icon-def';

export const FILE_ENV: IconDef = {
  viewBox: '0 0 24 24',
  body: fileIconBody({ color: '#ecd53f', label: 'ENV', textColor: '#000' }),
};
