import { fileIconBody } from './file-extension-icon';
import { IconDef } from './icon-def';

export const FILE_RS: IconDef = {
  viewBox: '0 0 24 24',
  body: fileIconBody({ color: '#dea584', label: 'RS', textColor: '#000' }),
};
