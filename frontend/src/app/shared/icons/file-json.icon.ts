import { fileIconBody } from './file-extension-icon';
import { IconDef } from './icon-def';

export const FILE_JSON: IconDef = {
  viewBox: '0 0 24 24',
  body: fileIconBody({ color: '#737373', label: '{ }' }),
};
